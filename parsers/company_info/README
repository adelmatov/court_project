# ОСНОВНЫЕ РЕЖИМЫ:
--source qamqor          # Из qamqor таблиц
--bins "БИН1,БИН2"       # Конкретные БИН
--file bins.txt          # Из файла

# МОДИФИКАТОРЫ:
--limit 100              # Ограничить количество
--force                  # Переспарсить существующие
--verify                 # Проверять через API
--dry-run                # Тест без сохранения
--log-level DEBUG        # Детальные логи

# ПРИМЕРЫ:
python -m parsers.company_info.main --source qamqor
python -m parsers.company_info.main --source qamqor --limit 100
python -m parsers.company_info.main --bins "060840008420"
python -m parsers.company_info.main --file bins.txt
python -m parsers.company_info.main --source qamqor --force
python -m parsers.company_info.main --source qamqor --limit 10 --dry-run